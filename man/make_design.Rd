% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{make_design}
\alias{make_design}
\title{Create an intercept-free design matrix}
\usage{
make_design(main_covariate, other_covariates = NULL, metadata)
}
\arguments{
\item{main_covariate}{Character string naming the primary covariate.}

\item{other_covariates}{Optional character vector naming additional
covariates to include in the design.}

\item{metadata}{Data frame-like object containing the covariates.}
}
\value{
A numeric design matrix. The model formula is stored in the
  \code{"formula"} attribute.
}
\description{
Builds a model matrix for a main covariate and optional adjustment
covariates, then verifies that the resulting design is full rank and has
unique coefficient names.
}
\examples{
metadata <- data.frame(
  group = factor(rep(c("control", "treated"), each = 4)),
  batch = factor(rep(c("A", "B"), 4))
)

design <- make_design(
  main_covariate = "group",
  other_covariates = "batch",
  metadata = metadata
)

}
